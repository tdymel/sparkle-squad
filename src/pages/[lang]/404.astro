---
import FourOFourPage from '../../components/pages/FourOFourPage.astro';
import { supportedLanguages } from '../../i18n';

export async function getStaticPaths() {
    return supportedLanguages.map(lang => ({
        params: { lang },
    }));
}

---

<FourOFourPage />